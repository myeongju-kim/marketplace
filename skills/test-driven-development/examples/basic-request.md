# Example Request

## Invocation
```text
/test-driven-development null-safe user registration validator를 구현하고, null 입력 시 IllegalArgumentException을 던지도록 작성
```

## Parameters
- `requirements`: null-safe user registration validator를 구현하고, null 입력 시 IllegalArgumentException을 던지도록 작성
